<p style="color: green"><%= notice %></p>

<h1 class="mt-5 display-5">Courses  <%= @courses.count %></h1>

<div id="courses">
  <% @courses.each do |course| %>
    <%= render course %>
    <p>
      <%= link_to "Show this course", course %>
    </p>
  <% end %>
</div>

<%= link_to "New course", new_course_path %>
